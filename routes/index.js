const main = require('./main');
const users = require('./users');
const books = require('./books');
const auth = require('./auth');
const cart = require('./cart');
const order = require('./order');
module.exports = [main, users, books, auth, cart, order];
